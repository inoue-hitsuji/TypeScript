**CSS** は HTML で書かれた文書の表示スタイルを定義する。CSS を HTML ファイルに適用するには `<link>` タグを指定する。複数の CSS ファイルを同じページに適用することもでき、その場合は `<link>` を続けて書く。CSS のメディアクエリを使うと、レスポンシブなページを作成することも可能である。

CSS は**セレクタ**と**プロパティ**を組み合わせて書く。セレクタはスタイルを適用する要素を指定し、プロパティは適用するスタイルの種類を指定する。

例：
```css
p {
  color: red;
  font-size: 16px;
}
```
この例では、`p` がセレクタ、`color` と `font-size` がプロパティである。

各スタイルの適用範囲は細かく指定することができる。ページ内の唯一の要素を指定するときは `id` 属性を、複数の要素をまとめて指定するときは `class` 属性を使う。セレクタは、複数セレクタや子孫セレクタで組み合わせることができる。

スタイルを指定する際は、ボックスモデルや `display` プロパティ、継承やスタイル適用の優先順位を考慮する。

すべての要素は**ボックス**をもち、内から順にコンテンツ、パディング、ボーダー、マージンの四層構造になっている。要素は、`display` プロパティの値により、ブロックレベル要素とインライン要素とに分類される。

文字のスタイルは親要素から子要素へ**継承**される。通常、継承されないスタイルもプロパティの値に `inherit` と書くと強制的に親要素のスタイルを継承できる。

スタイルの適用順位は、その定義される場所およびセレクタの**詳細度**で決まる。