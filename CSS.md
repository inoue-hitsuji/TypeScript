## CSS

**CSS** は HTML で書かれた文書のスタイルを定義する。スタイルというのは見え方のことである。CSS を HTML ファイルに適用するには `<link>` タグを指定する。複数の CSS ファイルを同じページに適用することもでき、その場合は `<link>` を続けて書く。

CSS は**セレクタ**と**スタイル**を組み合わせて書く。セレクタはスタイルを適用する要素を指定する。スタイルは**プロパティ**とその**値**によって構成される。

例：
```css
p {
  color: red;
  font-size: 16px;
}
```
この例では、`p` がセレクタ、`color` と `font-size` がプロパティで、`red` と `16px` がそれぞれプロパティで指定する値である。

### セレクタ

セレクタについて、スタイルをどの要素に適用するかは、次の〈セレクタの選択方法〉で指定する。

〈セレクタの選択方法〉
1. 要素の id 属性
2. 要素の class 属性
3. 要素名（タグの名前）
4. 1.~3. の組み合わせ
    （複数セレクタ、子孫セレクタ）

ページ内の唯一の要素を指定するときは `id` 属性を、複数の要素をまとめて指定するときは `class` 属性を使う。id セレクタは先頭に半角の `#` を、class セレクタは先頭に半角の `.` を書く。

### スタイルの指定

スタイルを指定する際は、ボックスモデルや `display` プロパティ、継承やスタイル適用の優先順位を考慮する。

- **ボックスモデル**
すべての要素は四角形の表示領域をもち、この表示領域をボックスと呼ぶ。ボックスは内から順に

    - コンテンツ
    - パディング
    - ボーダー
    - マージン
    
の四層構造になっている。

- **display プロパティ**
要素は、`display` プロパティの値により、**ブロックレベル**要素と**インライン**要素とに分類される。

- **継承**
文字のスタイルは親要素から子要素へ継承される。通常継承されないスタイルも、プロパティの値に `inherit` と書くと強制的に親要素のスタイルを継承できる。

- **スタイルの優先順位**
同じ要素に複数のスタイルが指定されているとき、スタイル適用の優先順位は、その定義される場所およびセレクタの詳細度で決定される。セレクタの詳細度は、値の後ろに`!important` を書いたスタイルが最優先で、それ以降は、〈セレクタの選択方法〉の順で、すなわち、id 属性（1）→ class 属性（2）→ 要素名（3）の順で適用される。
