# HTML & CSS

**HTML** は文書の構造を定義し、**CSS** は文書のスタイル（見え方）を定義する。**構造**とは、文書の要素の階層や意味的な関係性のことである。**スタイル**とは、色、サイズ、レイアウト、フォントなどの視覚的な表現のことである。HTML で定義された構造に対して、CSS でスタイルを適用することで、視覚的に表現された Web ページが完成する。

> HTML は文書をマークアップする。つまり、文字列をオブジェクトに変換する。

## HTML

HTML は、マークアップによって文書構造を定義する。**マークアップ**とは、テキストにタグや記号を追加して、文書の構造や意味を表現することである。**タグ**とは、要素の種類を表す記号（`<p>`、`<h1>`など）のことである。HTML では、テキストをタグで囲むことで、段落、見出し、強調などの構造や意味を定義する。定義することによって、検索エンジンが文書の構造を理解し、JS が要素を操作し、CSS がスタイルを適用できるようになる。

> 「マークアップ（markup）」の語源は、編集者が原稿に“印（mark）を書き込む（up）”ことから来ている。

> つまり、紙の原稿に赤字を入れたり、指示を書き加えたりして、文章の構造や意味を“明示するために記号を書き込む行為”を指していた。

> コンピュータの世界では、この概念がそのまま取り入れられ、文章の構造を記号で“注釈”する言語が「マークアップ言語」として発展した。

### タグと要素

タグで囲まれた部分を**要素内容**といい、タグと要素内容を合わせて**要素**という。要素内容の前（開始位置）にあるタグを**開始タグ**、要素内容の後（終了位置）にあるタグを**終了タグ**と呼ぶ。要素内容をもたない要素を**空要素**といい、空要素は開始タグのみ書く。また、開始タグの中に、要素の性質を表す**属性**を書くことができる。属性は `属性名="値"` の形式で記述する。

例：`<p>テキスト</p>` では、`<p>` が開始タグ、`テキスト` が要素内容、`</p>` が終了タグである。`<img src="image.jpg">` は空要素の例で、`src` が属性名、`"image.jpg"` が属性値である。

### 文書の構成

HTML の 1 行目には**文書型宣言**（DOCTYPE宣言）を書き、このページが HTML で書かれていることをブラウザに伝える。文書型宣言がないと、ブラウザが適切に HTML を解釈できない場合がある。HTML の 2 行目以降は `html` タグで全体を囲み、`html` の中に head と body を書く。**head** は文書情報を、**body** はブラウザの画面に表示する内容を表す。**文書情報**というのは、その文書に関するメタ情報（タイトル、概要説明文、文字コード、外部リソースへのリンクなど）のことである。

例：
```html
<!DOCTYPE html>
<html>
  <head>
    <title>ページタイトル</title>
  </head>
  <body>
    <p>本文</p>
  </body>
</html>
```

### コンテンツモデル

すべてのタグは 7 つのカテゴリーに分類され、その配置ルールは**コンテンツモデル**で定められている。コンテンツモデルは、どの要素がどの要素の中に配置できるかを定義することで、HTML 文書の構造を適切に保つための仕組みである。例えば、`<p>` 要素の中に `<div>` 要素を配置することはできない。

- メタデータコンテンツ
    > head 要素内に配置される
    - `<title>`：ページタイトル
    - `<meta>`：メタ情報
    - `<link>`：外部リソースへのリンク
    - `<style>`：CSS
    - `<script>`：JavaScript
- フローコンテンツ
    > body 要素内に配置される
    - `<div>`：汎用コンテナ
    - `<p>`：段落
    - `<ul>`, `<ol>`, `<li>`：リスト
    - `<blockquote>`：引用
    - セクショニングコンテンツ
        - `<article>`：独立した記事
        - `<section>`：セクション
        - `<nav>`：ナビゲーション
        - `<aside>`：補足情報
    - ヘッディングコンテンツ
        - `<h1>`〜`<h6>`：見出し
    - フレージングコンテンツ
        - `<span>`：インラインコンテナ
        - `<a>`：リンク
        - `<strong>`：強調
        - `<em>`：強調（斜体）
        - `<code>`：コード
    - エンベディッドコンテンツ
        - `<img>`：画像
        - `<audio>`：音声
        - `<video>`：動画
        - `<iframe>`：埋め込みフレーム
    - インタラクティブコンテンツ
        - `<a>`：リンク
        - `<button>`：ボタン
        - `<input>`：入力欄
        - `<select>`：選択リスト
        - `<textarea>`：テキストエリア

## CSS

CSS は HTML で書かれた文書のスタイルを定義する。スタイルというのは見え方のことである。HTML で定義された構造に対して、CSS でスタイルを適用することで、視覚的に表現された Web ページが完成する。

CSS を HTML ファイルに適用する方法には、以下の 3 つがある。

1. **外部 CSS ファイル**：`<link>` タグで外部の CSS ファイルを読み込む（推奨）
   ```html
   <link rel="stylesheet" href="style.css">
   ```
2. **`<style>` タグ**：HTML ファイル内に `<style>` タグで CSS を記述する
   ```html
   <style>
     p { color: red; }
   </style>
   ```
3. **インラインスタイル**：要素の `style` 属性に直接 CSS を記述する
   ```html
   <p style="color: red;">テキスト</p>
   ```

複数の CSS ファイルを同じページに適用することもでき、その場合は `<link>` を続けて書く。

CSS はセレクタとスタイルを組み合わせて書く。**セレクタ**はスタイルを適用する要素を指定する。**スタイル**とは、プロパティと値の組み合わせの集合のことである。**プロパティ**とは、スタイルの種類（色、サイズなど）を表す名前のことである。**値**とは、プロパティに設定する具体的な値のことである。

例：
```css
p {
  color: red;
  font-size: 16px;
}
```
この例では、`p` がセレクタ、`color` と `font-size` がプロパティで、`red` と `16px` がそれぞれプロパティで指定する値である。

### セレクタ

セレクタは、HTML で定義された要素を選択してスタイルを適用するための仕組みである。スタイルをどの要素に適用するかは、次の〈セレクタの選択方法〉で指定する。

〈セレクタの選択方法〉
1. 要素の id 属性
2. 要素の class 属性
3. 要素名（タグの名前）
4. 1.~3. の組み合わせ
    - **複数セレクタ**：カンマ（`,`）で区切って複数のセレクタを指定する
    - **子孫セレクタ**：スペースで区切って、親要素の子孫要素を指定する

ページ内の唯一の要素を指定するときは `id` 属性を、複数の要素をまとめて指定するときは `class` 属性を使う。id セレクタは先頭に半角の `#` を、class セレクタは先頭に半角の `.` を書く。

例：
```css
/* id セレクタ */
#header { color: blue; }

/* class セレクタ */
.title { font-size: 24px; }

/* 要素セレクタ */
p { color: red; }

/* 複数セレクタ */
h1, h2, h3 { font-weight: bold; }

/* 子孫セレクタ */
div p { color: green; }
```

### スタイルの指定

スタイルを指定する際は、ボックスモデルや `display` プロパティ、継承やスタイル適用の優先順位を考慮する。

- **ボックスモデル**
すべての要素は四角形の表示領域（ブラウザ上で要素が占める領域）をもち、この表示領域を**ボックス**と呼ぶ。ボックスは内から順に

    - **コンテンツ**：テキストや画像などの実際の内容が表示される領域
    - **パディング**：コンテンツとボーダーの間の余白
    - **ボーダー**：パディングの外側の境界線
    - **マージン**：ボーダーの外側の余白（他の要素との間隔）
    
の四層構造になっている。パディングとマージンは、レイアウトを調整するために使用する。

例：
```css
div {
  width: 200px;        /* コンテンツの幅 */
  padding: 10px;       /* パディング */
  border: 2px solid black; /* ボーダー */
  margin: 20px;        /* マージン */
}
```

- **display プロパティ**
要素は、`display` プロパティの値により、表示方法が決まる。主な値には以下のものがある。

    - **`block`**（ブロックレベル要素）：要素が横いっぱいに広がり、縦に並ぶ。`<div>`、`<p>`、`<h1>` などが該当する。
    - **`inline`**（インライン要素）：要素が横に並び、幅は内容に応じて決まる。`<span>`、`<a>`、`<strong>` などが該当する。
    - **`inline-block`**：インライン要素のように横に並ぶが、ブロック要素のように幅や高さを指定できる。

例：
```css
div { display: block; }
span { display: inline; }
button { display: inline-block; }
```

- **継承**
一部のプロパティ（`color`、`font-size`、`font-family` など）は、親要素から子要素へ**継承**される。継承されるプロパティは、子要素で指定しなくても親要素の値が適用される。一方、`margin`、`padding`、`border` などは継承されない。通常継承されないスタイルも、プロパティの値に `inherit` と書くと強制的に親要素のスタイルを継承できる。

例：
```css
body {
  color: blue;      /* 継承される */
  font-size: 16px;  /* 継承される */
  margin: 20px;      /* 継承されない */
}

p {
  color: inherit;   /* 親要素の color を継承 */
}
```

- **スタイルの優先順位**
同じ要素に複数のスタイルが指定されているとき、スタイル適用の優先順位は、その定義される場所およびセレクタの詳細度で決定される。優先順位は以下の通りである。

    1. **`!important`**：値の後ろに `!important` を書いたスタイルが最優先
    2. **定義される場所**：インラインスタイル > `<style>` タグ > 外部 CSS ファイル
    3. **セレクタの詳細度**：id 属性 > class 属性 > 要素名

例：
```css
/* 外部 CSS ファイル */
p { color: blue; }

/* 同じファイル内で */
p { color: red; }  /* 後から書かれた方が優先 */

/* インラインスタイル */
/* <p style="color: green;">テキスト</p> */  /* インラインスタイルが最優先 */

/* 詳細度 */
#header { color: blue; }    /* id セレクタ（優先度高） */
.title { color: red; }     /* class セレクタ */
p { color: green; }        /* 要素セレクタ（優先度低） */
```

